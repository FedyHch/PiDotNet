@model IEnumerable<Data.Models.tow>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.date_tow)
        </th>
       
        <th>
            @Html.DisplayNameFor(model => model.zone)
        </th>
      <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
       
        <td>
            @Html.DisplayFor(modelItem => item.date_tow)
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.zone)
        </td>
 
        <td>
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
